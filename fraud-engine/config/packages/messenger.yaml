framework:
    messenger:
        failure_transport: failed_transport

        serializer:
            default_serializer: messenger.transport.symfony_serializer
            symfony_serializer:
                format: json

        transports:
            # Consume: PaymentInitiatedMessage from Payment Gateway
            # Retry: 3 attempts with exponential backoff (1s, 2s, 5s)
            payment_initiated:
                dsn: '%env(RABBITMQ_DSN)%'
                options:
                    exchange:
                        name: payment.events
                        type: direct
                    queues:
                        payment.initiated.queue:
                            binding_keys: [payment.initiated]
                retry_strategy:
                    max_retries: 3
                    delay: 1000
                    multiplier: 2
                    max_delay: 32000

            # Publish: PaymentProcessedMessage (no consumer here)
            payment_processed:
                dsn: '%env(RABBITMQ_DSN)%'
                options:
                    exchange:
                        name: payment.events
                        type: direct
                        default_publish_routing_key: payment.processed
                    queues:
                        payment.processed.queue:
                            binding_keys: [payment.processed]

            # Failed messages (any retries exhausted)
            failed_transport:
                dsn: '%env(RABBITMQ_DSN)%'
                options:
                    exchange:
                        name: payment.events.dlq
                        type: direct
                        default_publish_routing_key: payment.failed
                    queues:
                        payment.failed.queue:
                            binding_keys: [payment.failed]

        routing:
            'App\Message\PaymentInitiatedMessage':  payment_initiated
            'App\Message\PaymentProcessedMessage':  payment_processed

