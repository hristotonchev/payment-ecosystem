services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Kernel.php'

    App\MessageHandler\PaymentNotificationHandler:
        arguments:
            $mailerFrom: '%env(MAILER_FROM)%'

